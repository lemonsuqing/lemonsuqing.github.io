<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>02嵌入式 Linux 编写第一个内核驱动 (Hello World)</title>
    <url>/2024/01/09/2%E7%BC%96%E5%86%99%E7%AC%AC%E4%B8%80%E4%B8%AA%E5%86%85%E6%A0%B8%E9%A9%B1%E5%8A%A8%20(Hello%20World)/</url>
    <content><![CDATA[编写第一个内核驱动 (Hello World)1. 核心概念：什么是内核模块 (.ko)？
裸机开发：你写好代码，编译成固件，烧进去。如果要修改一个 LED 的闪烁频率，你必须重新编译整个工程，重新烧录。
Linux 内核：内核非常庞大。如果你想加一个新硬件驱动，难道要重新编译几百兆的内核吗？不需要！
内核模块 (Kernel Module)：Linux 允许你把一部分代码编译成独立的 .ko 文件。
动态加载 (insmod)：系统运行时，把这个文件插进内核里，驱动立马生效。
动态卸载 (rmmod)：不用了就拔出来，释放内存。
这就像给电脑插拔 USB 鼠标一样方便，不需要重启系统。




2. 准备工作我们要创建一个独立的目录来存放驱动代码。不要混在内核源码或 U-Boot 源码里。
在 Ubuntu 终端执行：
cd ~/Embedded_studymkdir driver_hellocd driver_hello


3. 第一步：编写驱动源码 (hello_drv.c)使用 nano hello_drv.c 创建文件，输入以下代码。(请仔细阅读注释，每一行都是新知识)
#include &lt;linux/module.h&gt;   // 所有模块都需要的头文件#include &lt;linux/init.h&gt;     // 指定初始化和退出函数#include &lt;linux/kernel.h&gt;   // 包含 printk 等内核常用函数/*  * 1. 模块加载函数 (__init) * 当你执行 insmod 时，内核会调用这个函数。 * 类似于 C 语言的 main()，但它是内核视角的入口。 */static int __init hello_init(void)&#123;    // printk 是内核里的 printf。    // KERN_INFO 是日志级别，表示这是一条普通信息。    printk(KERN_INFO &quot;[Driver] Hello World! I am entering Kernel Space!\n&quot;);    return 0; // 返回 0 表示加载成功&#125;/*  * 2. 模块卸载函数 (__exit) * 当你执行 rmmod 时，内核会调用这个函数。 * 必须在这里把占用的资源（内存、中断、GPIO）释放掉，否则会导致内存泄漏。 */static void __exit hello_exit(void)&#123;    printk(KERN_INFO &quot;[Driver] Goodbye! I am leaving Kernel Space!\n&quot;);&#125;/*  * 3. 注册宏 * 告诉内核，上面哪两个函数是入口和出口。 */module_init(hello_init);module_exit(hello_exit);/*  * 4. 模块信息 (许可证) * 必须声明 GPL 协议，否则内核会报 &quot;Tainted&quot; (受污染) 警告，甚至拒绝加载。 */MODULE_LICENSE(&quot;GPL&quot;);MODULE_AUTHOR(&quot;LemonSuqing&quot;);MODULE_DESCRIPTION(&quot;A simple Hello World Module&quot;);


4. 第二步：编写 Makefile (构建规则)内核模块的编译非常特殊，它必须依赖 Linux 内核的源码树。因为驱动里调用的 printk 等函数，定义都在内核源码里。
创建 Makefile 文件 (注意文件名 M 大写)：nano Makefile
填入以下内容 (注意 Tab 缩进)：
# 1. 指定内核源码路径# 这里的路径必须是你之前编译过的那个 linux-5.15 的绝对路径！KDIR := /home/lemonsuqing/Embedded_study/linux-5.15# 2. 获取当前目录PWD := $(shell pwd)# 3. 核心变量：告诉内核构建系统，要把 hello_drv.c 编译成模块 (obj-m)obj-m := hello_drv.o# 4. 编译规则# -C $(KDIR): 切换到内核源码目录，借用它的 Makefile# M=$(PWD): 告诉内核，驱动源码在外面这个目录# modules: 编译模块的目标all:	make -C $(KDIR) M=$(PWD) modules ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf-clean:	make -C $(KDIR) M=$(PWD) clean


注意：make 命令下面那两行（make -C ...），必须以 Tab 键开头，不能用空格！


5. 第三步：编译驱动在 driver_hello 目录下执行：
make

预期结果：如果不报错，你会看到生成了一个 hello_drv.ko 文件。这就是我们的“驱动插件”！

6. 第四步：植入 SD 卡我们要把这个 .ko 文件放到 SD 卡的 RootFS 分区里，这样启动后的 Linux 才能访问到它。
(这是对之前 SD 卡挂载操作的复习)
cd ~/Embedded_study# 1. 挂载 SD 卡镜像 (会自动识别分区)sudo losetup -P -f --show sd.img# 假设是 /dev/loop0 (如果不是，请改号)# 2. 挂载 RootFS 分区 (分区2)sudo mount /dev/loop0p2 mount_root# 3. 复制驱动文件# 我们把它放到 /root 目录下sudo cp driver_hello/hello_drv.ko mount_root/root/# 4. 卸载sudo umount mount_rootsudo losetup -d /dev/loop0


7. 第五步：上机验证 (见证内核空间)启动 QEMU：
cd ~/Embedded_study/uboot_study/u-boot# 记得加 -sd 参数qemu-system-arm \    -M vexpress-a9 \    -m 512M \    -nographic \    -kernel u-boot \    -sd ~/Embedded_study/sd.img

U-Boot 启动命令 (还是老样子，如果没改 bootcmd 就手动输)：
# 从 SD 卡加载load mmc 0:1 0x60100000 zImageload mmc 0:1 0x62000000 vexpress-v2p-ca9.dtb# 启动参数 (SD卡启动)setenv bootargs &#x27;console=ttyAMA0,115200n8 debug earlyprintk root=/dev/mmcblk0p2 rootfstype=ext4 rw init=/init&#x27;# 启动bootz 0x60100000 - 0x62000000

进入 Linux Shell 后：

检查驱动文件在不在
cd /rootls

应该能看到 hello_drv.ko

加载驱动 (insmod)
insmod hello_drv.ko


现象：你应该立刻看到屏幕打印出 [Driver] Hello World! I am entering Kernel Space!。
如果没有直接打印，输入 dmesg 查看内核日志缓冲区。


查看已加载模块 (lsmod)
lsmod


现象：列表中应该有 hello_drv。


卸载驱动 (rmmod)
rmmod hello_drv


现象：屏幕打印出 [Driver] Goodbye! ...。




📝 知识点总结 (为什么这么做？)
User Space vs Kernel Space:

之前的 lemon_app (printf) 运行在用户空间。它崩溃了只影响它自己。
现在的 hello_drv (printk) 运行在内核空间。它拥有最高权限。**如果在这个代码里写个死循环或者空指针访问，整个系统（包括所有 APP）都会立刻死机。**这就是为什么驱动开发难。


Makefile 的魔法:

为什么编译驱动需要内核源码？因为 .ko 本质上是内核的一部分，它必须知道内核里的数据结构（如 struct module）长什么样。如果内核源码变了，驱动必须重编。


printk:

为什么不用 printf？因为 printf 是 C 标准库 (glibc/uclibc) 提供的，内核里没有标准库！内核只能用自己实现的 printk。




🚀 你的作业请完成上述步骤，并截图：

编译成功：在 Ubuntu 下 ls -l hello_drv.ko 的结果。
运行成功：在 QEMU 里执行 insmod 和 rmmod 时的打印信息。

这一步跑通，你就掌握了**“如何向运行中的 Linux 植入代码”**的核心技能。这通常是驱动开发工程师 80% 的日常工作流。
]]></content>
      <categories>
        <category>技术笔记</category>
      </categories>
      <tags>
        <tag>嵌入式</tag>
        <tag>Linux</tag>
        <tag>ARM</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2026/01/09/hello-world/</url>
    <content><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub.
Quick StartCreate a new post$ hexo new &quot;My New Post&quot;

More info: Writing
Run server$ hexo server

More info: Server
Generate static files$ hexo generate

More info: Generating
Deploy to remote sites$ hexo deploy

More info: Deployment
]]></content>
      <categories>
        <category>测试</category>
      </categories>
      <tags>
        <tag>test</tag>
      </tags>
  </entry>
  <entry>
    <title>01嵌入式 Linux 从零构建全栈手册 (基于 Vexpress-A9)</title>
    <url>/2024/01/09/1%E5%B5%8C%E5%85%A5%E5%BC%8F%20Linux%20%E4%BB%8E%E9%9B%B6%E6%9E%84%E5%BB%BA%E5%85%A8%E6%A0%88%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[📘 嵌入式 Linux 从零构建全栈手册 (基于 Vexpress-A9)目录索引：

环境搭建：地基与工具链
U-Boot 移植：编译、裁剪与设备树命令开启
Linux 内核：源码下载、配置、编译与“防卡死”处理
根文件系统：BusyBox 静态编译与 Init 脚本
应用开发：编写 C 程序并植入系统
SD 卡持久化：分区、格式化与脱机运行


第零章：环境搭建 (一切的开始)我们要在一个 x86 的 PC (Ubuntu) 上，生成能跑在 ARM 开发板上的代码。
0.1 安装基础工具打开 Ubuntu 终端，执行：
# 1. 更新软件源sudo apt update# 2. 安装构建工具 (Make, GCC, git, 解压工具等)sudo apt install build-essential libncurses-dev bison flex libssl-dev libelf-dev u-boot-tools qemu-system-arm git wget# 3. 安装交叉编译器 (核心工具)# 作用：把 C 代码翻译成 ARM 架构的汇编/机器码sudo apt install gcc-arm-linux-gnueabihf

0.2 创建工作目录结构为了防止文件乱放，我们需要统一规划。
# 回到家目录cd ~# 创建主工作目录mkdir Embedded_studycd Embedded_study# 创建 TFTP 服务器目录 (用于后续网络传输实验)# 后面 QEMU 会把这个目录映射为网络服务器的根目录mkdir ~/tftp_root


第一章：Bootloader (U-Boot)U-Boot 是系统的“第一棒”。它的核心任务是初始化 DDR 内存，并将内核搬运到内存中。
1.1 下载源码cd ~/Embedded_study# 克隆 U-Boot 官方仓库git clone https://github.com/u-boot/u-boot.gitcd u-boot# 切换到一个稳定的版本 (我们实验用的版本)git checkout v2023.07

1.2 配置与裁剪 (关键步骤)我们需要开启 fdt 命令，否则后续无法查看和操作设备树。

应用板级默认配置：
# 指定架构和编译器export ARCH=armexport CROSS_COMPILE=arm-linux-gnueabihf-# 加载 Vexpress 开发板配置make vexpress_ca9x4_defconfig
强制开启 fdt 命令 (最稳妥的方法)：

直接修改配置文件，防止在 Menuconfig 里找不着。

# 打开配置文件nano .config


按 Ctrl+W 搜索 CONFIG_CMD_FDT。
如果找不到，直接滑到文件最后一行，新起一行输入：CONFIG_CMD_FDT=y
按 Ctrl+O 回车保存，Ctrl+X 退出。



1.3 编译 U-Boot# -j8 表示用 8 个线程编译，速度快make -j8


产物：编译完成后，当前目录下会生成 u-boot (带调试信息) 和 u-boot.bin (二进制)。

1.4 启动与设备树实验 (核心原理)我们要在 U-Boot 里验证它是否能读取设备树。

启动 QEMU：
qemu-system-arm -M vexpress-a9 -m 512M -nographic -kernel u-boot
寻找设备树地址 (原理)：U-Boot 启动时会把内置的设备树（dtb）放在内存的某个位置。我们需要找到它。在 =&gt; 提示符下输入：
bdinfo


现象：输出中有一行 fdt_blob 或 fdt address，记录后面的十六进制地址（例如 0x608xxxxx 或 0x7fxxxxxx）。


挂载设备树：假设你上一步看到的地址是 0x6087d8b0 (请替换为你实际看到的)：
fdt addr 0x6087d8b0


原理：告诉 fdt 命令工具，“去这个内存地址解析数据”。


查看内容：
fdt print /


现象：屏幕打印出类似于 JSON 的树状结构。
意义：证明 U-Boot 功能正常，且具备解析硬件描述的能力。


退出 QEMU：按 Ctrl+A，松开，按 x。



第二章：Linux 内核 (Kernel)内核是系统的“大脑”。我们要下载源码，并解决两个著名的坑：“指令集不支持”和“启动卡死”。
2.1 下载源码 (v5.15)cd ~/Embedded_study# 使用清华源加速下载 (推荐)，或者直接用 kernel.org# --depth=1 表示只下载最近的提交，省时间省流量git clone --depth=1 -b v5.15 https://mirrors.tuna.tsinghua.edu.cn/git/linux.git linux-5.15cd linux-5.15

2.2 配置内核我们需要开启 Early Printk，否则内核启动时如果出错，会直接卡死且没有任何打印（沉默死亡）。

生成默认配置：
# 显式指定架构，防止环境变量失效make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- vexpress_defconfig
进入菜单开启调试：
make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- menuconfig


操作路径：
Kernel hacking —&gt;
Kernel low-level debugging functions (按 Y 选中)
Early printk (按 Y 选中)


保存并退出。



2.3 编译内核 (防坑指南)Ubuntu 新版 GCC 可能会把代码编译成旧版 ARM 指令，导致报错。我们必须强制指定指令集。
# zImage: 压缩内核# dtbs: 编译设备树# KCFLAGS: 强制指定 ARMv7-A 架构make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- zImage dtbs -j8 KCFLAGS=&quot;-march=armv7-a&quot;


产物：
内核：arch/arm/boot/zImage
设备树：arch/arm/boot/dts/vexpress-v2p-ca9.dtb




第三章：根文件系统 (RootFS)内核启动后需要运行程序，这些程序存放在文件系统里。我们用 BusyBox 制作一个最小系统。
3.1 下载与解压cd ~/Embedded_study# 下载源码wget https://busybox.net/downloads/busybox-1.36.1.tar.bz2# 解压tar -jxvf busybox-1.36.1.tar.bz2cd busybox-1.36.1

3.2 静态编译配置 (至关重要)如果不静态编译，程序在板子上运行时会找不到库文件（Error -2）。

打开配置菜单：make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- menuconfig
设置静态链接：
进入 Settings —&gt;
找到 Build static binary (no shared libs)，按 Y 选中变成 [*]。
保存退出。



3.3 编译与安装# 编译make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- -j8# 安装 (生成 _install 目录)make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- install

3.4 完善文件系统结构cd _install# 创建系统必须的目录mkdir -p dev proc sys tmp var etc home root

3.5 编写 Init 脚本 (PID 1)内核启动的第一个程序。
# 创建 init 文件nano init

填入以下内容：
#!/bin/sh# 挂载虚拟文件系统mount -t proc none /procmount -t sysfs none /sysmount -t tmpfs none /tmp# 打印信息echo &quot;-----------------------------------&quot;echo &quot;  Welcome to My Embedded Linux!&quot;echo &quot;-----------------------------------&quot;# 启动 Shell，永不退出exec /bin/sh

赋予权限：
chmod +x init


第四章：应用开发 (用户程序)我们要写一个 C 程序，让它开机自动运行。
4.1 编写与交叉编译cd ~# 创建 main.cnano main.c

内容：
#include &lt;stdio.h&gt;int main() &#123;    printf(&quot;\n[APP] Hello from LemonSuqing&#x27;s Application!\n\n&quot;);    return 0;&#125;

编译 (必须静态)：
arm-linux-gnueabihf-gcc main.c -o lemon_app -static

4.2 植入文件系统把这个程序放到我们刚才做的 RootFS 里。
cp ~/main.c ~/Embedded_study/busybox-1.36.1/_install/bin/lemon_app# 修正：应该是 cp ~/lemon_app ... cp ~/lemon_app ~/Embedded_study/busybox-1.36.1/_install/bin/

4.3 修改 Init 脚本调用它修改 ~/Embedded_study/busybox-1.36.1/_install/init，在 exec /bin/sh 之前加入：
/bin/lemon_app


第五章：打包与网络启动 (集成测试)5.1 制作 uRamdisk (RootFS 镜像)U-Boot 需要特定格式的文件系统镜像。
cd ~/Embedded_study/busybox-1.36.1/_install# 1. 归档并压缩find . | cpio -o --format=newc &gt; ../rootfs.imggzip -c ../rootfs.img &gt; ../rootfs.img.gz# 2. 加 U-Boot 头 (mkimage)# 产出文件放到 tftp 目录以便下载mkimage -A arm -O linux -T ramdisk -C gzip -n &quot;RootFS&quot; -d ../rootfs.img.gz ~/tftp_root/uRamdisk

5.2 搬运内核与设备树把编译好的内核和设备树也放到 TFTP 目录。
cd ~/Embedded_studycp linux-5.15/arch/arm/boot/zImage ~/tftp_root/cp linux-5.15/arch/arm/boot/dts/vexpress-v2p-ca9.dtb ~/tftp_root/

检查点：ls -l ~/tftp_root/ 应该有 zImage, vexpress-v2p-ca9.dtb, uRamdisk 三个文件。
5.3 启动 QEMU (网络模式)cd ~/Embedded_study/uboot_study/u-boot# 注意：tftp 路径必须用绝对路径qemu-system-arm \    -M vexpress-a9 \    -m 512M \    -nographic \    -kernel u-boot \    -net nic \    -net user,tftp=/home/lemonsuqing/tftp_root

5.4 U-Boot 启动命令在 U-Boot =&gt; 下依次输入：

下载文件 (地址要隔开，防止覆盖)：tftp 0x60100000 zImagetftp 0x61000000 uRamdisktftp 0x62000000 vexpress-v2p-ca9.dtb
设置启动参数 (告诉内核根文件系统在内存 ram0)：setenv bootargs &#x27;console=ttyAMA0,115200n8 debug earlyprintk root=/dev/ram0 rdinit=/init&#x27;
启动：bootz 0x60100000 0x61000000 0x62000000

成功标志：看到 Welcome to My Embedded Linux! 和 [APP] Hello... 的打印。

第六章：SD 卡持久化 (最终形态)网络启动断电数据就丢了。我们要把系统装进“虚拟 SD 卡”。
6.1 制作 SD 卡镜像退出 QEMU，回到 Ubuntu。

创建空文件 (512MB)：
cd ~/Embedded_studydd if=/dev/zero of=sd.img bs=1M count=512
分区 (fdisk)：
fdisk sd.img


输入 n -&gt; p -&gt; 1 -&gt; 2048 -&gt; +64M (创建分区1)
输入 n -&gt; p -&gt; 2 -&gt; 回车 -&gt; 回车 (创建分区2)
输入 t -&gt; 1 -&gt; c (修改分区1类型为 FAT32)
输入 w (保存)


格式化：
# 关联回环设备sudo losetup -f --show -P sd.img# 假设输出 /dev/loop0# 格式化 P1 (Boot, FAT32)sudo mkfs.vfat -F 32 -n &quot;BOOT&quot; /dev/loop0p1# 格式化 P2 (RootFS, EXT4)sudo mkfs.ext4 -L &quot;ROOTFS&quot; /dev/loop0p2

6.2 烧写文件# 挂载mkdir -p mount_boot mount_rootsudo mount /dev/loop0p1 mount_bootsudo mount /dev/loop0p2 mount_root# 复制内核与设备树到 Boot 分区sudo cp linux-5.15/arch/arm/boot/zImage mount_boot/sudo cp linux-5.15/arch/arm/boot/dts/vexpress-v2p-ca9.dtb mount_boot/# 复制文件系统到 RootFS 分区 (解压状态)# 注意：这里不是复制 uRamdisk，而是复制 _install 目录下的原始文件sudo cp -a busybox-1.36.1/_install/* mount_root/# 修正权限 (必须是 root)sudo chown -R root:root mount_root/# 卸载sudo umount mount_boot mount_rootsudo losetup -d /dev/loop0

6.3 SD 卡启动验证
启动 QEMU (插入 SD 卡)：
cd ~/Embedded_study/uboot_study/u-bootqemu-system-arm \    -M vexpress-a9 \    -m 512M \    -nographic \    -kernel u-boot \    -sd ~/Embedded_study/sd.img
U-Boot 操作：
# 1. 初始化 MMCmmc rescan# 2. 从 SD 卡读取文件到内存# load mmc &lt;设备&gt;:&lt;分区&gt; &lt;地址&gt; &lt;文件名&gt;load mmc 0:1 0x60100000 zImageload mmc 0:1 0x62000000 vexpress-v2p-ca9.dtb# 3. 设置启动参数 (关键修改)# root=/dev/mmcblk0p2: 告诉内核根文件系统在 SD 卡第 2 分区# rootfstype=ext4: 文件系统类型# rw: 可读写setenv bootargs &#x27;console=ttyAMA0,115200n8 debug earlyprintk root=/dev/mmcblk0p2 rootfstype=ext4 rw init=/init&#x27;# 4. 启动 (中间没有 Ramdisk 了，用 - 代替)bootz 0x60100000 - 0x62000000
持久化验证：进入系统后，执行：
echo &quot;Data Saved!&quot; &gt; /root/test.txt
强制重启 QEMU，再次进入系统，执行 cat /root/test.txt，如果内容还在，说明全栈开发实战圆满成功！


]]></content>
      <categories>
        <category>技术笔记</category>
      </categories>
      <tags>
        <tag>嵌入式</tag>
        <tag>Linux</tag>
        <tag>ARM</tag>
      </tags>
  </entry>
</search>
